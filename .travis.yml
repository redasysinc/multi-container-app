sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t forrestmoon/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true forrestmoon/react-test npm test

after_success:
  - docker build -t udemy/multi-client ./client
  - docker build -t udemy/multi-nginx ./nginx
  - docker build -t udemy/multi-server ./server
  - docker build -t udemy/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push udemy/multi-client
  - docker push udemy/multi-nginx
  - docker push udemy/multi-server
  - docker push udemy/multi-worker
