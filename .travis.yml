sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t forrestmoon/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true forrestmoon/react-test npm test

after_success:
  - docker build -t forrestmoon/udemy/multi-client ./client
  - docker build -t forrestmoon/udemy/multi-nginx ./nginx
  - docker build -t forrestmoon/udemy/multi-server ./server
  - docker build -t forrestmoon/udemy/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push forrestmoon/udemy/multi-client
  - docker push forrestmoon/udemy/multi-nginx
  - docker push forrestmoon/udemy/multi-server
  - docker push forrestmoon/udemy/multi-worker
